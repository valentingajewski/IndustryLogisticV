This root `configs/` folder is legacy reference content and is not part of the current LSOL runtime loader.

Current builds load player-editable runtime data only from `LSOL_Config/*.xml` and `LSOL_Config/missions/*.xml`.

Keep any migration or reference material here only if it is being used manually outside the game runtime.